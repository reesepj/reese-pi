{
  "name": "the-verifier-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pi Verifier Agent — read-only second agent that verifies the builder's claims and prompts back with concrete corrective feedback.",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@mariozechner/pi-coding-agent": "^0.70.5",
    "@mariozechner/pi-tui": "^0.70.5",
    "@types/node": "^22.0.0",
    "typebox": "^1.1.34",
    "typescript": "^5.6.0"
  }
}
